# گزارش تحویل پروژهٔ جزیرهٔ الفبا

## هدف این فایل

این گزارش برای هوش مصنوعی یا برنامه‌نویس بعدی نوشته شده است.

فایل زیپ را باز کن.

این گزارش را کامل بخوان.

سپس پروژه را از همین نقطه ادامه بده.

قبل از توسعهٔ جدید، پروژه را اجرا و خطاهای ساخت را اصلاح کن.

## وضعیت واقعی پروژه

این پروژه در حال حاضر نمونهٔ اولیهٔ فلاتر است.

نسخهٔ نهایی آموزشی نیست.

رابط اصلی، ذخیرهٔ سادهٔ پیشرفت، فونت‌ها و چند صفحهٔ نمونه ساخته شده‌اند.

بانک کامل محتوای ۲۲ درس هنوز ساخته نشده است.

تمرین‌های نمونه برای نشان دادن مسیر محصول ساخته شده‌اند.

## فایل‌های فعلی پروژه

فایل `lib/main.dart` هستهٔ فعلی برنامه است.

فایل `pubspec.yaml` وابستگی‌ها و فونت‌ها را ثبت می‌کند.

فایل `README.md` راهنمای کوتاه اجرای پروژه است.

فایل `.github/workflows/android.yml` گردش کار ساخت اندروید را تعریف می‌کند.

فایل `assets/fonts/Vazir-Regular.ttf` برای رابط کاربری است.

فایل `assets/fonts/wm_Tahriri.ttf` برای تمرین‌ها و گلیف‌های فارسی است.

## قرارداد فونت

تمام متن‌های رابط کاربری باید با فونت وزیر نمایش داده شوند.

منظور از رابط کاربری، عنوان‌ها، دکمه‌ها، پیام‌ها، منوها، گزارش‌ها و متن‌های راهنما است.

تمام محتوای تمرینی باید با فونت تحریری نمایش داده شود.

منظور از محتوای تمرینی، گلیف نشانه‌ها، حروف جعبه، واژه‌ها، جمله‌ها، کارت‌ها و متن داخل تختهٔ کلمه و جمله است.

نباید از فونت وزیر برای گلیف تمرینی استفاده شود.

نباید برای گلیف‌های تمرینی کادر تزئینی یا تکست‌باکس قابل مشاهده ساخته شود.

گلیف باید با فونت `wm_Tahriri.ttf` رندر شود.

فونت تحریری باید با متن راست‌به‌چپ و شکل‌دهی درست فارسی آزمایش شود.

فقط تعیین نام فونت کافی نیست.

باید خروجی واقعی روی دستگاه اندروید بررسی شود.

## چیزهایی که انجام شده است

صفحهٔ اصلی برنامه ساخته شده است.

سه دهکده در مسیر محصول تعریف شده‌اند.

دهکدهٔ حروف نمونهٔ ردگیری نشانهٔ «آ» دارد.

ردگیری با لمس صفحه دریافت می‌شود.

دکمهٔ پاک کردن وجود دارد.

دکمهٔ انجام دادم وجود دارد.

بازخورد موفقیت و تلاش دوباره وجود دارد.

تمرین نمونهٔ پیدا کردن «آ» میان گزینه‌ها ساخته شده است.

صفحهٔ فرم‌های نوشتاری نمونه ساخته شده است.

فرم‌های نمونه برای «سـ س» ثبت شده‌اند.

فرم‌های نمونه برای «نـ ن» ثبت شده‌اند.

فرم‌های نمونه برای «بـ ب» ثبت شده‌اند.

فرم‌های نمونه برای «مـ م» ثبت شده‌اند.

فرم‌های نمونه برای «ایـ یـ ی ای» ثبت شده‌اند.

یک موتور اولیه برای انتخاب فرم تنها، آغازین، میانی و پایانی نوشته شده است.

دهکدهٔ کلمه‌ها صفحهٔ نمونهٔ تختهٔ مغناطیسی دارد.

حالت درس‌محور نمونه دارد.

حالت کلمهٔ پیشنهادی نمونه دارد.

حالت نوشتار آزاد نمونه دارد.

دهکدهٔ جمله‌ها سه سطح ابتدایی، میانی و کل کتاب دارد.

نمونهٔ جملهٔ «بابا آب داد.» وجود دارد.

ذخیرهٔ سادهٔ پیشرفت با `shared_preferences` اضافه شده است.

فونت وزیر برای رابط ثبت شده است.

فونت تحریری برای تمرین ثبت شده است.

فایل ساخت اندروید برای GitHub Actions اضافه شده است.

## چیزهایی که هنوز انجام نشده است

بانک محتوایی کامل کتاب فارسی ساخته نشده است.

بانک محتوایی کامل کتاب نگارش ساخته نشده است.

تقویم محتوایی همهٔ درس‌ها هنوز به دادهٔ برنامه تبدیل نشده است.

واژه‌های هر درس هنوز کامل استخراج و اعتبارسنجی نشده‌اند.

جمله‌های هر سطح هنوز به حداقل ۳۰ جمله نرسیده‌اند.

نگاره‌های ۱ تا ۱۰ هنوز به تمرین دیجیتال تبدیل نشده‌اند.

تمام تمرین‌های کتاب نگارش هنوز پیاده‌سازی نشده‌اند.

تمرین «چی مثل چی» کامل نشده است.

فلش‌کارت کامل با تصویر و صدا ساخته نشده است.

موتور اتصال فارسی هنوز نسخهٔ آموزشی و کامل نیست.

ویژگی اتصال واقعی هر نشانه باید برای همهٔ حروف ثبت شود.

حرکت‌ها، تشدید، نیم‌فاصله و استثناهای فارسی هنوز کامل نشده‌اند.

منطق «می» هنوز باید با دادهٔ آموزشی حل شود.

واژه‌نامهٔ کامل پایهٔ اول وجود ندارد.

تصاویر اختصاصی واژه‌ها هنوز آماده نشده‌اند.

فایل‌های صوتی هنوز تولید نشده‌اند.

گزارش کامل والدین هنوز ساخته نشده است.

آزمون خودکار و تست رابط هنوز کامل نشده است.

تست واقعی روی دستگاه اندروید هنوز باید انجام شود.

قبل از انتشار عمومی باید مجوز استفاده از محتوای کتاب بررسی شود.

## برنامهٔ ادامهٔ توسعه

### مرحلهٔ اول: سالم‌سازی پروژه

پروژه را با فلاتر پایدار اجرا کن.

دستور `flutter pub get` را اجرا کن.

دستور `flutter analyze` را اجرا کن.

دستور `flutter test` را اضافه و اجرا کن.

خطاهای کامپایل را اصلاح کن.

خروجی آزمایشی اندروید بساز.

فونت وزیر و فونت تحریری را روی دستگاه واقعی بررسی کن.

### مرحلهٔ دوم: معماری محتوا

محتوا را از رابط کاربری جدا کن.

برای درس، نشانه، فرم گلیف، واژه، جمله، تمرین و پیشرفت مدل داده بساز.

هر محتوا باید شناسهٔ ثابت داشته باشد.

هر واژه باید `firstLesson` داشته باشد.

هر جمله باید `level` و `allowedLesson` داشته باشد.

هیچ محتوایی نباید زودتر از درس مجاز نمایش داده شود.

### مرحلهٔ سوم: ساخت بانک درس اول

درس اول را کامل استخراج کن.

نشانه‌های مجاز درس اول «آ، ا، بـ، ب» هستند.

واژه‌های درس اول را فقط از کتاب و نگارش مرتبط انتخاب کن.

تمرین ردگیری را برای نشانه‌های درس اول بساز.

تمرین پیدا کردن نشانه را برای نشانه‌های درس اول بساز.

واژه‌های تصویری درس اول را اضافه کن.

قبل از رفتن به درس دوم، درس اول را روی اندروید آزمایش کن.

### مرحلهٔ چهارم: موتور گلیف

برای هر نشانه فرم تنها، آغازین، میانی و پایانی تعریف کن.

برای نشانه‌های جداشونده، شکل اتصال را غیرفعال کن.

تشخیص جایگاه باید بر اساس دادهٔ فرم باشد.

از تبدیل متن به عکس برای کلمهٔ کامل استفاده نکن.

برای نمایش تک‌گلیف می‌توان از متن با فونت تحریری استفاده کرد.

برای نمایش واژهٔ متصل، شکل‌دهی فارسی باید درست بررسی شود.

اگر فونت در اتصال خودکار فرم درست را تولید نمی‌کند، گلیف‌های فرم‌دار را به‌صورت دادهٔ آموزشی جدا تعریف کن.

### مرحلهٔ پنجم: دهکدهٔ کلمه‌ها

جعبهٔ نشانه‌ها را بر اساس درس فعال فیلتر کن.

نشانه‌ها را بدون کادر قابل مشاهده نشان بده.

تخته باید راست‌به‌چپ باشد.

کاربر باید بتواند نشانه‌ها را جابه‌جا کند.

کاربر باید بتواند نشانه را حذف کند.

حالت درس‌محور باید پاسخ هدف مشخص داشته باشد.

حالت پیشنهادی باید واژهٔ هدف را از بانک بخواند.

حالت آزاد نباید واژهٔ ناشناخته را شکست اعلام کند.

### مرحلهٔ ششم: دهکدهٔ جمله‌ها

برای هر سطح حداقل ۳۰ جمله بساز.

هر جمله را با سطح و درس مجاز برچسب بزن.

چند پاسخ صحیح را در صورت نیاز پشتیبانی کن.

نقطه و نشانه‌گذاری را جدا از واژه‌ها مدیریت کن.

در هر نوبت پنج تمرین نشان بده.

تمرین‌های دشوار را دوباره در مسیر دانش‌آموز برگردان.

### مرحلهٔ هفتم: صوت و تصویر

برای هر نشانه، واژه و جمله فایل صوتی جدا تولید کن.

فایل صوتی باید بدون موسیقی و با تلفظ واضح باشد.

نام‌گذاری پیشنهادی:

`lesson_01_letter_a.mp3`

`lesson_01_word_ab.mp3`

`lesson_01_sentence_001.mp3`

فایل‌ها را در پوشه‌های جدا قرار بده.

برنامه باید بدون صوت هم بدون خطا اجرا شود.

تصاویر کتاب را مستقیم کپی نکن.

برای هر واژه تصویر اختصاصی یا دارای مجوز تولید کن.

### مرحلهٔ هشتم: پیشرفت و گزارش

آخرین درس بازشده را ذخیره کن.

تمرین‌های ناقص را ذخیره کن.

نشانه‌های دشوار را ذخیره کن.

تلاش‌ها و پاسخ‌های درست را ثبت کن.

صفحهٔ گزارش والدین بساز.

گزارش باید ساده و توصیفی باشد.

از رتبه‌بندی آزاردهنده برای کودک استفاده نکن.

### مرحلهٔ نهم: انتشار

فونت‌ها را در خروجی نهایی بررسی کن.

حالت راست‌به‌چپ را روی دستگاه واقعی آزمایش کن.

اندازهٔ لمس را حداقل ۴۴ پیکسل نگه دار.

خروجی apk بساز.

GitHub Actions را اجرا کن.

فایل apk را از بخش Artifacts بررسی کن.

## فایل‌هایی که باید همراه پروژه به هوش مصنوعی بعدی داده شوند

### ضروری

فایل زیپ همین پروژه.

فایل `aval-farsi-1404.pdf`.

فایل `aval-negareshe-farsi-1404.pdf`.

فایل `wm_Tahriri.ttf`.

فایل `Vazir-Regular.ttf`.

### لازم برای بازسازی ظاهر

فایل `island-map.svg`.

فایل `map-island-3.svg`.

فایل لوگوی برنامه.

هر تصویر اختصاصی که بعداً تولید می‌شود.

### لازم برای تکمیل صوت

فایل‌های صوتی نشانه‌ها.

فایل‌های صوتی واژه‌ها.

فایل‌های صوتی جمله‌ها.

### لازم برای انتشار

نام نهایی برنامه.

شناسهٔ بستهٔ اندروید.

نام مخزن GitHub.

تصمیم دربارهٔ مجوز استفاده از محتوای کتاب.

## نکتهٔ مهم دربارهٔ فایل کتاب

کتاب‌های درسی حقوق نشر دارند.

برای انتشار عمومی برنامه باید وضعیت مجوز استفاده از متن، تصویر و تمرین‌ها بررسی شود.

برای نمونهٔ خصوصی و توسعهٔ داخلی، فایل‌های ارائه‌شده برای تحلیل محتوا استفاده می‌شوند.

برای انتشار عمومی، تصویرسازی اختصاصی و محتوای دارای مجوز انتخاب کن.

## تعریف پایان پروژه

پروژه زمانی آمادهٔ انتشار است که همهٔ ۲۲ درس محتوای کنترل‌شده داشته باشند.

هر سه دهکده با محتوای کامل کار کنند.

فونت رابط وزیر باشد.

تمام تمرین‌ها با فونت تحریری باشند.

اتصال حروف روی دستگاه واقعی درست باشد.

پیشرفت دانش‌آموز بعد از بستن برنامه باقی بماند.

برنامه بدون اینترنت اجرا شود.

خروجی apk از GitHub Actions با موفقیت ساخته شود.

تست‌های اصلی سبز باشند.

## دستور شروع برای هوش مصنوعی بعدی

ابتدا فایل زیپ را باز کن.

سپس این گزارش را کامل بخوان.

بعد پروژه را با فلاتر اجرا کن.

ابتدا خطاهای فعلی را اصلاح کن.

بعد بانک محتوای درس اول را از دو فایل PDF بساز.

از درس دوم جلوتر نرو تا درس اول با فونت وزیر و فونت تحریری روی اندروید تأیید شود.

پس از تأیید داخلی، درس‌ها را یکی‌یکی اضافه کن.

بدون حذف قابلیت‌های موجود توسعه بده.

هر مرحله باید قابل اجرا و قابل تست باقی بماند.

## هشدار فنی مهم

این بسته سورس اصلی فلاتر را دارد، اما پوشه‌های تولیدشدهٔ پلتفرم مانند `android/` در بستهٔ فعلی وجود ندارند.

هوش مصنوعی بعدی باید ابتدا با `flutter create .` اسکلت پلتفرم را تولید کند یا یک پروژهٔ فلاتر کامل بسازد.

پس از آن باید فایل `android/app/src/main/AndroidManifest.xml`، شناسهٔ بسته و تنظیمات ساخت بررسی شوند.

فایل GitHub Actions فعلی، قبل از این مرحله تضمین‌شده نیست و باید بعد از تولید اسکلت اندروید آزمایش شود.
